<div class="form-background">
  <h1>Login</h1>
  <%= form_for @session.user, url: session_path, method: 'post' do |f| %>
    <div class="padding login-form">
      <p class="error"><%= @session.general_error %></p>

      <%= f.label 'Username' %>
        <span class="error"><%= @session.username_error %></span>
      <%= f.text_field :username, class: 'textfield', autofocus: true %>
      <p></p>
      <%= f.label 'Password' %>
        <span class="error"><%= @session.password_error %></span>
      <%= f.password_field :password, class: 'textfield' %>


    </div>
    <div class="button-background">
      <%= f.submit 'LOGIN', class: 'register-button' %>
    </div>
  <% end %>

</div>