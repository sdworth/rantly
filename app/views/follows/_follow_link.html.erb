<% unless is_followed?(user) %>
  <%= link_to 'Follow', follow_path(user), method: :post, class: 'follow-link' %>
<% else %>
  <%= link_to 'Unfollow', follow_path(user), method: :delete, class: 'unfollow-link' %>
<% end %>